import data from "./generatedUnifiedDetails.json";

/** Pre-generated unifiedDetail for all built-in seed cards (id → text). */
export const BUILTIN_UNIFIED_DETAILS: Record<string, string> = data as Record<string, string>;
