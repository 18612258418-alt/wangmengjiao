# Design QA — 搜索页添加资料菜单

## 对照范围

- 参考：用户提供的搜索输入框“+”展开菜单截图。
- 实现：搜索首页输入框及其左下方添加资料菜单。

## 检查结果

- 结构：通过。菜单锚定输入框左下方展开，不再直接以居中弹窗作为第一层反馈。
- 展开状态：通过。“+”在展开后切换为关闭图标，且可再次点击收起。
- 视觉：通过。白色圆角浮层、轻阴影、纵向菜单、分组分隔线与参考图一致。
- 内容：通过。菜单仅保留“上传文件、添加图片或扫描件、自动获取学习资料”；已删除重复的录音入口与粘贴文本入口。
- 交互：通过。“上传文件”进入放大的居中资料弹窗；“添加图片或扫描件”直接进入相机取景页，并在底部提供“拍板书、相册、扫文档”；“自动获取学习资料”进入原有同步设置。上传、解析和同步逻辑未改变。
- 弹窗：通过。资料弹窗宽度为视口的 60%、高度为视口的 72%，保持居中并保留完整内容。
- 控制台：通过。展开菜单并进入添加资料窗口时无错误日志。

## 剩余差异

- P3：参考图菜单更宽、更高；当前版本按 Memo 现有字号与输入框宽度做了比例适配，避免遮挡下面的建议问题。

final result: passed

---

# Design QA — 搜索回答对话化

## 检查结果

- 对话层级：通过。用户问题以右侧主色气泡展示，Memo 回答以左侧头像和白色回答气泡展示。
- 内容归属：通过。“接下来可以做”和“记忆依据”作为本次 Memo 回答的后续卡片排列在同一消息列中。
- 输入连续性：通过。底部大胶囊输入框保持固定，可继续追问。
- 响应式：通过。用户气泡和回答气泡设置最大宽度，在窄屏下可自然收缩。
- 构建及控制台：通过。构建成功，页面验证无错误日志。

final result: passed

---

# Design QA — 扫描与搜索回答页

## 对照范围

- 参考：用户提供的搜索回答页截图，以及前序统一的大胶囊输入框样式。
- 实现：相机内“扫文档”状态、搜索回答页和回答页底部输入区。

## 检查结果

- 扫描入口：通过。“扫文档”不再调起独立文件选择，直接切换相机扫描状态。
- 扫描反馈：通过。页面显示文档扫描框、四角定位线、扫描线和放置提示；拍摄后执行透视矫正。
- 回答结构：通过。已移除左右分栏，回答正文、后续操作和记忆依据按单列顺序呈现。
- 后续操作：通过。三个操作保持可点击，并以一行操作卡展示。
- 记忆依据：通过。保留展开/收起、来源类型、标题、日期及原文定位交互。
- 底部输入：通过。结果页恢复与搜索首屏一致的大胶囊形态，并保留加号、语音状态和发送按钮。
- 构建：通过。`npm run build` 成功。
- 控制台：通过。验证搜索与扫描状态时无错误日志。

## 剩余差异

- 无 P0/P1/P2 问题。

final result: passed
